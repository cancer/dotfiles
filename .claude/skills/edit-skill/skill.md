---
name: edit-skill
description: 既存スキルを編集する。影響調査付き
argument-hint: <skill-name> <変更内容>
allowed-tools: Read, Edit, Write, Glob, Grep
user-invocable: true
---

既存スキルを編集します。

## 引数
$ARGUMENTS

## 引数の形式

```
<スキル名> <変更内容の説明>
```

- **スキル名**: 対象スキルの名前（ディレクトリ名）
- **変更内容の説明**: 変更指示（自然言語）

## スキルファイルの配置

- ユーザースコープ: `~/.claude/skills/<skill-name>/skill.md`
- プロジェクトスコープ: `.claude/commands/<skill-name>.md`

## 実行手順

### 1. 対象スキルの確認

対象スキルのファイルを読み込み、現在の内容を把握する。存在しない場合はエラーを報告して終了。

### 2. 影響調査

対象スキルの名前で、他のスキルからの参照を検索する。Grepツールを使用：

- `~/.claude/skills/` 配下の全 `*.md` ファイルからスキル名を検索
- `.claude/commands/` 配下の全 `*.md` ファイルからスキル名を検索（存在する場合）

以下の参照パターンを確認する：
- 他のスキルの本文中での名前の言及（例: 「`comment-issue` スキルを使って」）
- `allowed-tools` に `Skill` を含むスキルからの呼び出し可能性

調査結果を報告する：
- **参照あり**: どのスキルからどのように参照されているかを一覧で示す
- **参照なし**: 他スキルへの影響なしと報告

### 3. 編集

1. 影響調査の結果を踏まえ、変更内容が他スキルに影響を与えないか確認
2. 指示された変更を適用する
3. 変更後の内容を表示して確認を促す

## 注意事項

- 指示された変更のみを適用し、それ以外の箇所を変更しないこと
- 影響調査は省略しないこと
